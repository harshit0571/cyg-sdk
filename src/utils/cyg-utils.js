export const validateSignature = (body, signature, secret) => {
  // Implement signature validation
  return true;
};

export const normalizeDate = (date) => {
  // Implement date normalization
  return new Date(date).toISOString();
};
